import React from 'react';

function QueryCity({ value, onChange }: { value: string; onChange:(_: string) => void }) {
  return (
    <div>
      QueryCity
    </div>
  );
}

export default QueryCity;
